{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Tuner Execution Task Schema",
  "type": "object",
  "properties": {
    "pipeline_id": {
      "type": "string",
      "description": "Identifier for the target YAML in Library."
    },
    "input_data_list": {
      "type": "array",
      "description": "The list of names of the input files for this run.",
      "items": { "type": "string" }
    }
  },
  "required": [
    "pipeline_id",
    "input_data_list"
  ]
}