{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Tuner Execution Task Schema",
  "type": "object",
  "properties": {
    "pipeline_id": {
      "type": "string",
      "description": "Identifier for the target YAML pipeline definition inside the Library."
    },
    "library_repository_url": {
      "type": "string",
      "description": "The full Git URL for the Library repository containing the pipeline definitions."
    },
    "version_tag": {
      "type": "string",
      "description": "Specific Git branch, tag, or commit hash to ensure reproducible execution."
    }
  },
  "required": ["pipeline_id", "library_repository_url", "version_tag"]
}