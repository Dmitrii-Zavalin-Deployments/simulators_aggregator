name: "ACE Pipeline Tuner Engine"

on:
  push:
    branches:
      - "**"
  workflow_dispatch:
  schedule:
    # Hourly Pulse: Wakes up at the top of every hour to execute a batch
    - cron: '0 * * * *'

jobs:
  gatekeeper:
    name: "🛑 Engine Gatekeeper"
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.check.outputs.status }}
    steps:
      - name: 🗂️ Checkout Repository
        uses: actions/checkout@v4

      - name: 🔍 Check Dormancy Flag
        id: check
        run: |
          FLAG_PATH="dormant.flag"
          if [[ "${{ github.event_name }}" == "schedule" ]]; then
            if [ -f "$FLAG_PATH" ] && grep -q "STATUS: DORMANT" "$FLAG_PATH"; then
              echo "status=dormant" >> $GITHUB_OUTPUT
              echo "🛑 ENGINE DORMANT: Halting hourly pulse execution."
            else
              echo "status=active" >> $GITHUB_OUTPUT
              echo "🚀 ENGINE ACTIVE: Proceeding with execution pulse."
            fi
          else
            echo "status=active" >> $GITHUB_OUTPUT
            echo "🚀 AUTO-WAKE: Manual or Push trigger detected. Activating Engine."
          fi

  # pre_flight_tests:
  #   name: "🧪 Pre-Flight: Logic & Hygiene"
  #   needs: gatekeeper
  #   if: needs.gatekeeper.outputs.status == 'active'
  #   runs-on: ubuntu-latest
  #   env:
  #     PYTHONPATH: ${{ github.workspace }}
  #     FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  #     TEST_COMMAND: "pytest -s tests/ -vv --cov=src --cov-report=term-missing"
    
  #   steps:
  #     - name: 🗂️ Checkout Repository
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     # 🔧 GLOBAL ROBOT IDENTITY + NOTES INITIALIZATION (Branch-Isolated)
  #     - name: 🤖 Initialize Robot Identity & Notes
  #       run: |
  #         git config --global user.name "Automation Engine [${{ github.ref_name }}]"
  #         git config --global user.email "automation-${{ github.ref_name }}@yourdomain.com"
  #         git config --global notes.rewriteRef "refs/notes/*"

  #     - name: 🐍 Setup Python 3.11
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"

  #     - name: 📦 Install Dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements.txt

  #     - name: 🧹 Auto-Clean (Autoflake)
  #       run: autoflake --in-place --recursive --remove-unused-variables --remove-all-unused-imports src tests

  #     - name: 🧹 Static Analysis & Lint (Ruff)
  #       run: ruff check src tests --fix

  #     - name: 🚀 Commit & Push Hygiene Changes + Sync Notes
  #       if: always()
  #       run: |
  #         git add .
  #         if [ -n "$(git status --porcelain)" ]; then
  #           git commit -m "🤖 robot: auto-clean hygiene update [Engine]"
  #           TARGET_COMMIT=$(git rev-parse HEAD)

  #           # 1️⃣ Fetch remote notes FIRST
  #           git fetch origin refs/notes/commits:refs/notes/commits || true

  #           # 2️⃣ Add note locally
  #           git notes add -f -m "robot: true" "$TARGET_COMMIT"

  #           # 3️⃣ Push commit
  #           git push origin HEAD

  #           # 4️⃣ Force-push notes
  #           git push --force origin refs/notes/commits
  #         else
  #           echo "No hygiene changes detected."
  #         fi

  #     - name: 💀 Dead Code Audit (Vulture)
  #       id: vulture
  #       run: vulture src --min-confidence 70

  #     - name: 🧪 Quality Gate Check (Rule 2)
  #       if: failure() && steps.vulture.outcome == 'failure'
  #       run: |
  #         echo "CRITICAL: Rule 2 (Zero-Debt Mandate) violation."
  #         echo "Unused code detected in src/. Delete unreachable logic before continuing."
  #         exit 1

  #     - name: 🧪 Run Logic & Scientific Verification
  #       run: |
  #         echo "🚀 Initiating: $TEST_COMMAND"
  #         $TEST_COMMAND
      
  #     - name: 🔍 Forensic Audit
  #       if: always()
  #       run: |
  #         if [[ "${{ job.status }}" == "success" ]]; then
  #           echo "✅ All systems nominal. No anomalies detected."
  #           cat <<EOF > debug/forensic_audit.sh
  #         #!/bin/bash
  #         # Description: Automated forensic audit for ACE solver failures.
  #         # Status: Dormant (All systems nominal)
  #         exit 0
  #         EOF
  #           chmod +x debug/forensic_audit.sh
  #           echo "🧹 Diagnostic script reset to Dormant mode."
  #         else
  #           echo "⚠️ CI Failure Detected. Initiating Deep Forensic Audit..."
  #           echo "============================================================"
  #           echo "ls -R output:"
  #           ls -R
  #           echo "============================================================"
  #           echo "debug/forensic_audit.sh run results:"
  #           if [ -f "debug/forensic_audit.sh" ]; then
  #             bash debug/forensic_audit.sh
  #           else
  #             echo "❌ Critical: Diagnostic script 'debug/forensic_audit.sh' not found."
  #           fi
  #           echo "============================================================"
  #           echo "📉 RE-RUNNING FAILED TEST FOR FINAL SIGNAL"
  #           $TEST_COMMAND || true
  #           exit 1
  #         fi
      
  #     - name: 🧹 Sync Forensic Reset + Sync Notes
  #       if: success()
  #       run: |
  #         git add debug/forensic_audit.sh
  #         if [ -n "$(git status --porcelain debug/forensic_audit.sh)" ]; then
  #           git commit -m "🤖 robot: forensic reset [PASS]"
  #           TARGET_COMMIT=$(git rev-parse HEAD)

  #           # 1️⃣ Fetch remote notes FIRST
  #           git fetch origin refs/notes/commits:refs/notes/commits || true

  #           # 2️⃣ Add note locally
  #           git notes add -f -m "robot: true" "$TARGET_COMMIT"

  #           # 3️⃣ Push commit
  #           git push origin HEAD

  #           # 4️⃣ Force-push notes
  #           git push --force origin refs/notes/commits
  #         else
  #           echo "Forensic script already dormant."
  #         fi

  production_run:
    name: "🚀 Production: ACE Loop Execution"
    # needs: pre_flight_tests
    needs: gatekeeper
    if: needs.gatekeeper.outputs.status == 'active'
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
      DROPBOX_APP_KEY: ${{ secrets.DROPBOX_APP_KEY }}
      DROPBOX_APP_SECRET: ${{ secrets.DROPBOX_APP_SECRET }}
      DROPBOX_REFRESH_TOKEN: ${{ secrets.DROPBOX_REFRESH_TOKEN }}
      DROPBOX_FOLDER: "simulators"
      TUNER_STATE_ARCHIVE: "tuning_${{ github.ref_name }}.zip"
      WORKSPACE_DIR: "data/testing-input-output/tuning_${{ github.ref_name }}"
    
    steps:
      - name: 🗂️ Checkout Engine
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 🔧 GLOBAL ROBOT IDENTITY + NOTES INITIALIZATION (Branch-Isolated)
      - name: 🤖 Initialize Robot Identity & Notes
        run: |
          git config --global user.name "Automation Engine [${{ github.ref_name }}]"
          git config --global user.email "automation-${{ github.ref_name }}@yourdomain.com"
          git config --global notes.rewriteRef "refs/notes/*"

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install Engine Pip Dependencies
        run: pip install -r requirements.txt
      
      # 📥 STATE IMPORT: Check if a prior state zip exists on Dropbox
      - name: 📥 Check Pipeline State Existence
        id: import_state
        run: |
          python3 src/io/state_manager.py \
            --folder "${{ env.DROPBOX_FOLDER }}" \
            --filename "${{ env.TUNER_STATE_ARCHIVE }}" >> $GITHUB_OUTPUT
      
      - name: 🔍 Log Pipeline State Status
        run: echo "Current state status is [${{ steps.import_state.outputs.state_status }}]"

      # 📥 WARM START: Download and unpack state archive if found
      - name: 📥 Download Existing State Archive
        if: steps.import_state.outputs.state_status == 'found'
        run: |
          python3 src/io/download_from_dropbox.py \
            --folder "${{ env.DROPBOX_FOLDER }}" \
            --filename "${{ env.TUNER_STATE_ARCHIVE }}"
          
          mkdir -p data/testing-input-output
          unzip -o data/testing-input-output/${{ env.TUNER_STATE_ARCHIVE }} -d data/testing-input-output/

      # 🐍 ENV CAPABILITY: Provisions Conda environment for executing simulator modules
      - name: 🐍 Set up Conda Environment
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: "3.10"
          channels: conda-forge
          channel-priority: strict
          activate-environment: tuner-env

      - name: 📦 Cache Conda Environment
        id: cache-conda
        uses: actions/cache@v4
        with:
          path: /usr/share/miniconda/envs/tuner-env
          key: ${{ hashFiles('tasks/**') }}-${{ github.ref_name }}
          restore-keys: |
            ${{ hashFiles('tasks/**') }}-${{ github.ref_name }}-

      # ⚙️ COLD START ONLY (Initialization & Parameter Explosion)
      - name: ⚙️ Initialize Pipeline State (Cold Start)
        if: steps.import_state.outputs.state_status == 'not_found'
        shell: bash -el {0}
        run: |
          echo "📥 Cold Start Detected: Scaffolding baseline files..."
          mkdir -p repositories
          rm -rf repositories/fluid_dynamics_simulator
          git clone https://github.com/Dmitrii-Zavalin-Deployments/fluid_dynamics_simulator.git repositories/fluid_dynamics_simulator
          
          if [ ! -d "repositories/fluid_dynamics_simulator/.git" ]; then
              echo "❌ ERROR: Repository clone failed."
              exit 1
          fi
          echo "✅ Repository staged successfully."

          echo "🚀 Running state initialization logic..."
          conda activate tuner-env

          # Build the command dynamically without risking the Python string evaluation trap
          CMD="python3 src/pipeline/initialize_state.py --repo-path repositories/fluid_dynamics_simulator"
          
          if [ "${{ steps.cache-conda.outputs.cache-hit }}" == "true" ]; then
              echo "💡 Conda cache hit achieved. Appending --cached-dependency to skip setup scripts."
              CMD="$CMD --cached-dependency"
          fi
          $CMD

      - name: 🧮 Explode Configuration Range Matrix (Cold Start)
        if: steps.import_state.outputs.state_status == 'not_found'
        shell: bash -el {0}
        run: |
          python3 src/pipeline/matrix_exploder.py \
            --config-path "${{ env.WORKSPACE_DIR }}/configs/mesh_config_0970b4d.json" \
            --output-path "${{ env.WORKSPACE_DIR }}/config_combinations_array.json"

      
      - name: ⚙️ Initialize Pipeline State (Warm Start)
        # Assuming you want to trigger this when state is 'found' (the opposite of your cold start)
        if: steps.import_state.outputs.state_status == 'found' 
        shell: bash -el {0}
        env:
          # Dynamically resolve directory based on the current branch (e.g., main, develop, feature-x)
          WORKSPACE_DIR: "data/testing-input-output/tuning_${{ github.ref_name }}"
        run: |
          echo "📥 Warm Start Detected: Initializing from $WORKSPACE_DIR..."
          
          # 1. Safety check: ensure the dynamic path actually exists
          if [ ! -f "$WORKSPACE_DIR/state.json" ]; then
            echo "❌ ERROR: State file not found at $WORKSPACE_DIR/state.json"
            exit 1
          fi

          # 2. Generate the command string using the dynamic environment variable
          PROVISION_CMD=$(python3 src/pipeline/generate_provision_cmd.py --state-file "$WORKSPACE_DIR/state.json")
          
          # 3. Audit
          echo "=========================================================="
          echo "GENERATED COMMAND:"
          echo "$PROVISION_CMD"
          echo "=========================================================="
          
          # 4. Execute
          eval "$PROVISION_CMD"
          
          if [ $? -eq 0 ]; then
            echo "✅ Repositories staged successfully."
          else
            echo "❌ ERROR: Repository staging failed."
            exit 1
          fi

          echo "🚀 Running state initialization logic..."
          conda activate tuner-env
          
          # Note: Ensure this path is also dynamic if your repo destination depends on branch
          CMD="python3 src/pipeline/initialize_state.py --repo-path repositories/mesh_generator"
          
          if [ "${{ steps.cache-conda.outputs.cache-hit }}" == "true" ]; then
              CMD="$CMD --cached-dependency"
          fi
          $CMD

      # # 🚀 PROCESSING LOOPS: Process a discrete tuning batch
      # - name: 🚀 Execute Tuning Batch Pulse
      #   if: steps.import_state.outputs.state_status == 'found'
      #   id: execution_pulse
      #   shell: bash -el {0}
      #   run: |
      #     conda activate tuner-env
      #     python3 src/pipeline/execute_batch.py \
      #       --workspace "${{ env.WORKSPACE_DIR }}" \
      #       --batch-size 1 >> $GITHUB_OUTPUT

      # # 🎁 CLOSURE RUN ONLY: Triggered when queue is empty on run entry
      # - name: 🎁 Assemble SaaP Deliverables & Set Dormancy
      #   if: steps.import_state.outputs.state_status == 'found' && steps.execution_pulse.outputs.tuning_done == 'true'
      #   shell: bash -el {0}
      #   run: |
      #     python3 src/pipeline/deliverable_assembly.py --workspace "${{ env.WORKSPACE_DIR }}"
          
      #     echo "STATUS: DORMANT" > dormant.flag
      #     git add dormant.flag
      #     git commit -m "🤖 robot: pipeline execution fully depleted | transition to DORMANT"
      #     git push origin HEAD

      # 📤 STATE PERSISTENCE: Packaging and uploading updates to Dropbox
      - name: 📤 Package and Upload State Snapshot
        if: always()
        run: |
          if [ -d "${{ env.WORKSPACE_DIR }}" ]; then
            echo "📦 Packing active state folder..."
            cd data/testing-input-output/
            zip -r "${{ env.TUNER_STATE_ARCHIVE }}" "tuning_${{ github.ref_name }}"
            cd ../..
            
            echo "📤 Uploading updated transaction state to Dropbox..."
            python3 src/io/upload_to_dropbox.py \
              --folder "${{ env.DROPBOX_FOLDER }}" \
              --filename "${{ env.TUNER_STATE_ARCHIVE }}"
          fi

      # 🔍 INTEGRITY ENFORCEMENT
      - name: 🔍 Run Forensic Audit
        if: always()
        shell: bash -el {0}
        run: |
          if [ -f "debug/forensic_audit.sh" ]; then
            chmod +x debug/forensic_audit.sh
            bash debug/forensic_audit.sh
          fi

      # - name: 📥 Import Control Plane (Dropbox Sync)
      #   env:
      #     APP_KEY: ${{ secrets.DROPBOX_APP_KEY }}
      #     APP_SECRET: ${{ secrets.DROPBOX_APP_SECRET }}
      #     REFRESH_TOKEN: ${{ secrets.DROPBOX_REFRESH_TOKEN }}
      #   run: ./src/io/download_from_dropbox.sh

      # - name: ⚡ Execute Sovereign ACE Logic Gate (Pulsed Batch)
      #   env:
      #   run: python3 src/main.py

      # - name: 📊 Sync Audit, Ledger & Dormancy State + Sync Notes
      #   if: always()
      #   run: |
      #     git add performance_audit.md dormant.flag config/orchestration_ledger.json
          
      #     # CHECK IF THESE SPECIFIC FILES HAVE CHANGES
      #     if ! git diff --cached --exit-code; then
      #       git stash
      #       git pull origin HEAD --rebase
      #       git stash pop
            
      #       # Re-add after rebase to ensure clean state
      #       git add performance_audit.md dormant.flag config/orchestration_ledger.json
      #       git commit -m "🤖 robot: ACE cycle complete | status=${{ job.status }}"
      #       TARGET_COMMIT=$(git rev-parse HEAD)

      #       # 1️⃣ Fetch remote notes FIRST
      #       git fetch origin refs/notes/commits:refs/notes/commits || true

      #       # 2️⃣ Add note locally
      #       git notes add -f -m "robot: true" "$TARGET_COMMIT"

      #       # 3️⃣ Push commit
      #       git push origin HEAD

      #       # 4️⃣ Force-push notes
      #       git push --force origin refs/notes/commits
      #     else
      #       echo "No core state changes detected."
      #     fi